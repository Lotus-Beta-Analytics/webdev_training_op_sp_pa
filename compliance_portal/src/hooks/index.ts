import { useInitiatives, useAddInitiative } from "./initiative";
import { useTypes } from "./type";
import { useStatuses } from "./status";
import { useUsers } from "./user";

export default {
  useInitiatives,
  useAddInitiative,
  useTypes,
  useStatuses,
  useUsers,
};
